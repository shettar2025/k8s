apiVersion: v1
kind: Service
metadata:
  name: studentapp-service
spec:
  type: NodePort
  selector:
    app: studentapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007
